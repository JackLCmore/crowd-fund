<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>Project Title:{{name}}</h2>
  </div>
  <div class="col-md-6">
    <p>Date Created:{{date_created}}</p>
    <p>{{description}}</p>
  </div>
  <div>
    <p>{{#if needed_funding}} This Project Needs Funding! {{/if}}</p>
  </div>
</div>